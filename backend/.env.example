# Backend Environment Variables
PORT=3001
NODE_ENV=development

# Database URL (MongoDB Atlas connection string)
# Replace with your actual MongoDB Atlas database URL
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bookit_db?retryWrites=true&w=majority
